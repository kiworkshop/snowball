import styled from 'styled-components';
import { Button, Table as AntdTable, Modal as AntdModal } from 'antd';

export const StockTransactionButton = styled(Button)`
  margin-right: 10px;
`;

export const Table = styled(AntdTable)`
  margin-top: 20px;
`;

export const Modal = styled(AntdModal)``;
